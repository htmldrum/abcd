package main

func StartServer(){
	// Bind to port given by env (assum 6942)
	// Bind to resource routes
	// Respond to JSON only
	// Bounce concurrent attempts as concurrency controls implemented as local file lock
}
